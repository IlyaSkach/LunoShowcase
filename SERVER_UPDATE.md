# Инструкция: Обновление сервера

## Шаг 1: Подготовка файлов на локальном компьютере

На твоём Mac выполни:

```bash
cd /Users/ilas/projects/LUNO-shop/bot
npm run build
```

Это создаст обновлённые файлы в папке `bot/dist/`.

---

## Шаг 2: Копирование файлов на сервер

### Вариант A: Через rsync (рекомендуется)

На Mac выполни:

```bash
cd /Users/ilas/projects/LUNO-shop
rsync -avz bot/dist/ app@79.174.84.164:~/luno-promo-bot/dist/
```

При первом подключении введи `yes` для подтверждения.

### Вариант B: Через scp

На Mac выполни:

```bash
cd /Users/ilas/projects/LUNO-shop
scp -r bot/dist/* app@79.174.84.164:~/luno-promo-bot/dist/
```

---

## Шаг 3: Подключение к серверу

### Через SSH (если настроен):

```bash
ssh app@79.174.84.164
```

### Или через консоль Reg.ru:

1. Зайди в панель управления Reg.ru
2. Открой консоль для твоего VPS
3. Войди под пользователем `app`

---

## Шаг 4: Проверка файлов на сервере

После подключения к серверу выполни:

```bash
cd ~/luno-promo-bot
ls -la dist/
```

Убедись, что файлы обновились (проверь дату изменения).

---

## Шаг 5: Перезапуск бота

На сервере выполни:

```bash
pm2 restart luno-promo-bot
```

Или с обновлением переменных окружения:

```bash
pm2 restart luno-promo-bot --update-env
```

---

## Шаг 6: Проверка работы

### Проверь статус:

```bash
pm2 status
```

Должен быть статус `online` для `luno-promo-bot`.

### Проверь логи:

```bash
pm2 logs luno-promo-bot --lines 30
```

Должны появиться строки:
- `🚀 API сервер запущен на порту 3001`
- `🤖 Бот @lunoshowcase_bot запущен`

### Проверь, что бот отвечает:

Отправь боту `/start` в Telegram и проверь, что он отвечает.

---

## Шаг 7: Тестирование QR-переходов

1. Открой ссылку через Telegram:
   - `https://t.me/lunoshowcase_bot/promotion?startapp=qr_promo`
   - `https://t.me/lunoshowcase_bot/chat?startapp=qr_chat`

2. Проверь логи на сервере:
   ```bash
   pm2 logs luno-promo-bot --lines 50
   ```
   
   Должны появиться строки:
   - `📥 QR-переход получен:` с данными пользователя

3. Проверь Google таблицу — должны появиться записи с данными пользователя.

---

## Если что-то пошло не так

### Бот не запускается:

```bash
pm2 logs luno-promo-bot --err --lines 50
```

### Проверь, что порт свободен:

```bash
lsof -i :3001
```

Должен быть только процесс `luno-promo-bot`.

### Перезапусти бота:

```bash
pm2 restart luno-promo-bot
```

### Если нужно полностью перезапустить:

```bash
pm2 stop luno-promo-bot
pm2 start luno-promo-bot
```

---

## Быстрая команда (всё сразу)

Если всё настроено, можно выполнить на Mac:

```bash
cd /Users/ilas/projects/LUNO-shop/bot && \
npm run build && \
rsync -avz dist/ app@79.174.84.164:~/luno-promo-bot/dist/ && \
ssh app@79.174.84.164 "cd ~/luno-promo-bot && pm2 restart luno-promo-bot"
```

---

## Что изменилось на сервере

1. ✅ Добавлено логирование QR-переходов (видно, что приходит в запросе)
2. ✅ Обновлена обработка команды `/start` с параметрами `promo_qr` и `chat_qr`
